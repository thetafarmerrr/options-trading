# 签到任务单模板

> 签到时读取此文件。**每条操作必须写完整命令 + 路径 + 判据 + 下一步动作。**
> 用户拿到任务单→照做→回来报结果。不允许出现"见某文件"的跳转——所有信息内联。

## 输出格式

```
## 📋 YYYY-MM-DD 签到 · 周X

### 🔴 纪律计数器：0 / 20（7/10 归零：分腿顺序反+裸空）
### 💧 干旱计数器：X 天（≥5→回顾参数，≥10→系统排查）

### 四道门速览
| 门 | 进度 |
|----|------|
| T | 6/20 |
| D | 0/20 🔴 |
| Data | 5/60 |
| K | 🟢 已过 |

---

| # | 时段 | 做什么 | 具体操作 |
|---|------|--------|---------|
| ☀️ | 8:00-8:25 | **英语·四步**（盘前清醒期）| ①视频：打开 Mike 播放列表https://youtube.com/playlist?list=PLPVve34yolHY43YaBegHMzN9WjrTnQfFr 从上次停的地方继续 1 集。看画面+字幕抓大意→张嘴跟读不停。不重播不查词。②挖矿：抓 3 个整句，追加到 `glossary.md` 底部 `## 句型库`。格式：`- 整句  （来源）`。③炸弹：张嘴 90 秒说刚看的内容——不停、不改、不查词。录音→语音转文字→瞥一眼差异。④笔译：把昨天的市场结论翻译成英文 2-3 句（当天 Scanner 还没跑，用昨天的）。**放盘前原因：张嘴需脑力+勇气，清醒期黄金** |
| 0 | 8:25-8:45 | **盘前启动** | ① `python3 tools/iv_collector.py --watch`（常驻到 14:50 自动三采：9:15+9:30+14:50）② `./start_deep_otm.sh`（盘前 discover 换月+启动采集）——**盘前不跑 scanner**（8/14 棉花 +2.1% 污染实锤：盘前盘口=昨收快照+未稳，8:48 价差 295%）。决策只用 9:15 采完那次输出 |
| 0b | 8:45-9:00 | **轻量清空** | Douglas（读 1 段+写一句「今天有没有这个心理」）+ 翻文档 4 min（轮换表见下）——不依赖数据，盘前顺手清空，给下午减负 |
| 1 | 9:15-9:30 | **开盘决策窗口** | ① 9:15 iv_collector 自动采完 → 执行 `python3 -m tools.scanner.main` ② **四层独立讲清**（8/27 起）：拿输出**先自己写 10 品种各一行「做/不做 + 一句核心理由」**（5 min，不翻文档不问我——理由跟规则一致就算对，能抓住关键层即可）→ 贴教练对答案 ③ **IV 方向预测**（8/27 起，低配）：有 [EXEC]/纸面信号的品种 → 预测 5 交易日后 ATM IV 升/降 → append `data/iv_direction_pred.csv`（只升/降不做平；对照=5 日后主采 ATM IV vs 今天；**对照日写在 `compare_date` 列**——T 日与 T+5 日两列并排，不用心算；到对照日填 actual/correct）
   - **reason 冻结铁律（9/10 立）**：`user_reason` / `coach_reason` 一经写入，**对照日之前一律不改**。发现漏了事件/理由写错 → 写进 journal 或另起备注，**不回头改原文**——改了等于掺入后见之明，把一个**可评分的预测**变成**不可评分的解释**。错就留痕
   - **`actual_reason` 独立填（9/10 立）**：到对照日填 actual 时，**不打开 `user_reason` / `coach_reason` 两列**，只依据当下观察到的实际走势与原因写 `actual_reason`。列位置在 `actual` 之后。本规则自 9/7 批（对照日 9/14）起启用
     - **原则上对照日当天写，不攒批**（9/10 立）：`actual_reason` 在 `compare_date` 当天写＝正当时（记忆新、看不到后续批次、事件还热）。**攒批补写是例外**：8/27-9/2 五批经用户指示事后补写，**已逐行标 `〔跨期补写 9/10〕`**——补写行的证据条件（记忆衰减／跨批可见易拼整齐故事／本地记录过期）**弱于当天写**，读时不与当天写的等价看待。这是**读法**标注，不影响计分列
     - **`actual_reason` 写什么（9/10 立）**：写**事件/基本面 → IV 传导机制**，**不是复述 Δ**（Δ 已在 `actual_delta_pp` 列，复述=同义重复=零信息）。格式：`[事件·基本面] → [传导段] → [IV 结果]`。**允许且鼓励用搜索**核实事件（本地 `weekly_event_scan.json` 是快照、会过期——9/10 实测 i 条目过期 4 天、sr 条目漏报广西台风），**搜索与本地记录冲突时以搜索为准并在条目内标 ⚠️修正**。**没有事件就写"无已证实催化"，不编因果**（违者=9/10 传导跳跃那条 mistakes）
   - **平（flat）口径（9/10 立）**：`|Δ| < 0.25pp` → `actual = flat`，**`user_correct` / `coach_correct` 留空、不计分**（剔出分母）。**阈值来源=实算，不是拍脑袋**：50 个已回填样本的 ATM 盘口隐含 IV 半宽中位 **0.141pp** / P75 **0.197pp** → 0.25pp 略高于 P75 = 落在测量噪声内。全样本 **10/60（17%）**落此带（sr×4 / c×2 / rm×3 / cf×1）。**同时新增 `actual_delta_pp` 列**（位置在 `actual` 之后）存原始 Δ——每个 flat 必须可追溯到具体数字（数据诚实第1条）。**防自欺**：报准确率时**必须同时报 flat 数**，写成 `67%（剔平 43 例）`，不许只报分子分母好看的那个（否则这个口径就变成"判不准就免责"的逃生口）
   - **回测影响（8/27-9/2 五批）**：先验符号——重算 Δ 与原 `actual` 标签 **50/50 完全一致**（证明当初就是这个口径，回填没换尺子）。套用后：user 66%→**67%**、coach 62%→**67%**，**两侧都往 67% 收**——噪声带此前在随机惩罚 coach 更多，剔掉不是偏袒谁
   - **当前全样本（6 批 60 行，9/10 脚本打印）**：已回填 60 · flat 10 · 计分 50 → **user 33/50 = 66% · coach 33/50 = 66%**。报数格式固定为 `66%（剔平 50 例，flat 10）`
   - **历史回填**：8/27-9/2 五批共 7 行已改标 flat。改的是**测量口径**，不是**预测内容**——`user_pred` / `coach_pred` / `user_reason` / `coach_reason` 四列一字未动
④ 再用新输出决策（盘前不跑 scanner，8/14 脏数据教训）：有 [EXEC]→复制出来，进六步进场（monitoring-rules.md 第三节）；无→记干旱天数。**这是真正的决策锚点** |
| 1b | 9:00-9:30 | **纸面挑信号** | Scanner PAPER 区（买方价差/跨式/单腿），挑 1-2 个记到 `docs/paper-tracker.md`。**判据三条**：① IV 分位低（P30↓ 买方便宜）② 盈亏比≥5:1 ③ 近期事件/趋势配合。按星级（⭐~⭐⭐⭐）记一行。15 秒。周六复盘回看 |
| 3 | 9:30-10:15 | **Drill B**（决策后第一个·易拖优先）| `python3 tools/drill_system.py B`。每天打底。报正确率和速度。**位置原因：重复肌肉不需要脑力巅峰，放盘前是浪费清醒；易拖项锚定决策窗口后第一个，防拖没** |
| 3b | 9:30-10:15 | **Drill 轮转 ×4** | `python3 tools/drill_system.py <模块>`。每天 4 个模块（B 之外），约 20 分钟。**周一** E+G+I+A | **周二** E+C+H+I | **周三** E+G+D+J | **周四** E+C+H+F | **周五** E+G+D+A | **周六** G+D+C+J | **周日** D+E+H+F。报分数。B+4 合计 25 分钟 |
| 3c | 9:30-10:15 | **D-Drill** | `python3 tools/d_drill.py`。42 题。答「做/不做」+一句原因（写规则内容不是编号）。**每题选之前问「反证？」**——Ne 模式匹配抢在 Ti 验证之前 = INTP 第一漏洞。不跳这步 |
| 4a | 10:15-11:00 | **Sinclair A**（问题驱动） | 带今天 Scanner 的问题找教练。教练把问题压缩成一句话 → 翻书定位到那一页 → 读到答案发回来 → 教练接上今天信号拆解。**不从头读、不读 60 分钟**——只读能回答当前问题的那一段。覆盖 Ch3-6。读完 journal 写：概念 + 今天哪个信号验证了它。**位置原因：A 依赖当天 Scanner 问题，9:00 后才来** |
| 4e | 10:15-11:00 | **Sinclair B**（日志倒灌·每天） | 教练读近 3 天 journal→挑一个决策→搜 PDF 确认→指页码（Ch7-12）。翻到那页读 2-3 页→回来报告对应关系。每天跑。进度表在 `memory/quant-plan-state.md` §Sinclair B |
| 2 | 14:50 | **IV 收盘自动** | --watch 自动采。⚠️ 收盘后部分品种断流，14:50 有 buffer。买方视角：IV 分位 <30% 品种=黄金窗口，对比早盘变化写进 journal 一句 |
| 4d | 15:00-15:30 | **身体**（20 min） | 壶铃 S&S（摇摆×100 + 土耳其起立×10），壶铃到货前徒手三样（深蹲×20 + 俯卧撑×10 + 平板 60s）×3 轮。不改动作不改顺序——零选择。收盘后起身，把僵坐解开。journal 写清醒度 1-5 |
| 5 | 15:30-16:00 | **影响力 + 视频脚本（9/1 起）** | Scanner+IV 跑完→检查有无可发，有→60 秒内发，不发就没了。收盘 IV 有发现→当天第二条（日上限 ≤2）。5 分钟/条，格式 1-2 句中文+数据点。视频脚本：教练读昨日 `journal/YYYY-MM-DD.md` → 90s 脚本（钩子+三段+结尾）→ 用户只录不发（录完 30 秒内关，**不看回放不重录**）→ **每日验收**：录完转文字贴教练，教练核对脚本三点讲全没（全→✅ 关账；缺→指缺哪个点明天补）。**不审判质量**（口音/节奏/表情不管），只核三点。优先级=9 月最后，15:30 被交易/训练拖住时**先砍视频**，不砍训练/认知考核 |
| 6 | **18:00 硬截止** | **收尾** | ① `python3 tools/daily_quiz.py` 3 题验收→② journal 按 **`docs/journal-template.md`** 结构写（必写三段：今日反思·两行自问「今天我有没有主动放弃的收益机会？（包括没去争取的）怕麻烦/回避少赚了吗？」＋ 明日待办·明天必做 1-2 件＋ 今日学到·用户写）→③ 训练分数、Scanner 结论写进 `journal/YYYY-MM-DD.md`→④ **IV 数据健康**：`python3 -c "import csv; rows=list(csv.DictReader(open('data/iv_history.csv'))); d=sorted(set(r['date'] for r in rows if r.get('date'))); vc={v:len({x['date'] for x in rows if x.get('variety')==v and x.get('date')}) for v in {x['variety'] for x in rows if x.get('variety')}}; print('最新:', d[-1], '| 总唯一:', len(d)); print('每品种:', vc); print('Data门=最小:', min(vc.values()), '→', [k for k,n in vc.items() if n==min(vc.values())])"` 确认今日有 morning+afternoon；**Data 门进度=每品种最小唯一日期**（非总唯一，8/31 曾混口径误记）→⑤ 确认 deep_otm 两文件有今日数据 `wc -l data/deep_otm_trading.csv data/deep_otm_eod.csv`→⑥ `git add -A && git commit -m "YYYY-MM-DD" && git push`。**18:00 前必须 commit，防"再跑一次 scanner"拖没闭环** →⑦ **卖方窗口归类**（9/8 立）：今天有卖方 EXEC 信号 → `docs/seller-window-tracker.md` 当月流水 append 一行（A开/B绿未开/C毙+唯一主毙层；**写不出唯一硬规则层 → 归 B 不许归 C**）。无 EXEC 跳过 →⑧ **回填类操作必须核文件**（9/10 立）：凡"回填/补写/更新某 CSV 某批"的操作，**判据是文件不是记忆**——收尾时跑一次实填行数核对，与叙述里的数字对齐。**没跑核对 = 没回填**。账目类结论（几分之几）**必须由脚本打印，不许手写**（手写=凭记忆复述=数据诚实第 1 条失守）。反面教材：9/3 批 IV 回填当天只算没写，成绩却已写进 commit/memory，CSV 10 行全空 |


> 特殊行——干旱 ≥3 天时插入：
| * | **无信号日操作** | 打开 `trade_log.md`，随机抽一笔历史交易，重读进场离场逻辑。问自己：现在回头看当时该做吗？写一行答案进 journal。或打开 `docs/tail-events.md` 随机选一个事件，用今天的 IV 数据推演如果今天发生会怎样 |

> 周六额外行：
| *** | **🔍 Ti-Ne 锚点**（≥30 min） | 本周最困扰/最不安的一个问题→从数据里挖根因。不能是读新东西。锚点是周六的精神引擎——没找到可以跳 Si 任务，找到了其他快过 |
| * | **文档全扫** | 四文档全过一遍（monitoring-rules + glossary + mistakes + paper-tracker），8 min。查本周漏了什么、哪篇需要更新 |
| * | **语言学碎片** | 教练推送一个「为什么英语是这样」的点，10 min 读完，决定存不存 glossary |
```

## 签到时必须做到

1. 从 memory 读取最新数字（纪律计数、干旱天数、门进度、训练分）填入模板
2. 根据当天周几确定 Drill 轮转模块
3. 干旱 ≥3 天 → 插入无信号日操作行
4. 周六 → 插入语言学碎片 + 碎片整理行
5. 不出现「见某文件」——所有操作细节内联

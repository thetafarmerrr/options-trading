# 2026-09-16 周三 · FOMC D-0 + Excel 毁数据事故

## 交易

### 开仓
无。零真实持仓（cf2701 虚拟仓已于 9/11 触发离场六步第 5 条平仓，结算 −¥205）。

### Scanner / IV
- **09:15 采集 → scanner 跑在有效表下**（`weekly_event_scan.json` valid_until 9/19 ✅）。
- **2 个 [EXEC] 卖方信用价差，全毙，零 override**：
  - **PTA ta2611 卖P6800/6700** 净收 ¥38.0 rr 0.61 —— 三层独立毙：① 事件层 FOMC D-0 + EIA（ta 在映射内）② **倒挂·法则①**（近 37.9% > 远 32.4%）③ 🔮补偿推定·geo
  - **白糖 sr2701 卖P5200/5100** 净收 ¥21.5 rr 0.27 —— 三层独立毙：① **事件层不绿且窗口就在当天**（桂南 9/16-18 暴雨，对照日当天）② 🔮补偿推定·weather ③ rr 0.27 贴 0.25 红线
- 其余 8 品种无 EXEC（豆粕 50 条被 `IV-HV<1%` 拦截；PTA 三档 rr<0.25 落 NEAR）。
- **四层独立讲清：10 品种「都不做」**。决策规则内、零 override → 纪律计数器 **38/20**。
  ⚠️ 留痕：答复仅「都不做」三字，**未给 10 行逐条理由**。四层独立讲清要求的「各一行核心理由」本日缺。判据本身对，过程缺一层——不改分，记录在案。
- **IV 方向预测（T=9/16 → compare_date 2026/9/23）**：教练首次执行**盲填铁律**（`coach_pred`/`coach_reason` 在她提交前落盘，09:2x）。教练 2 up / 8 down，用户 7 up / 3 down —— 大分歧。
- IV-HV 一览（09:15）：沪金 +7.1% 🔺 / PTA +6.8% 🔺 / 白糖 +1.2% / 橡胶 +0.7% / 玉米 +0.1% / 铁矿石 −0.1% / 甲醇 −0.1% / 豆粕 −1.0% / 棉花 −2.0% / 菜籽粕 −4.5% 🔻
- 5dΔ 极值：PTA **+4.7pp**（上） / 甲醇 **−5.2pp**（下）。

### 纸面信号
【待填】Scanner PAPER 区本日给出：白糖 sr2701 买方价差 4 档（RR 6.5–12.95:1）+ 买单腿趋势触发 2 个（棉花买P13000 OTM18.8%、白糖买P4800 OTM10.2%）。**你挑了哪几个？三条判据写了吗？**

### 持仓
空仓（真实 + 虚拟皆无）。
⚠️ `memory/quant-plan-state.md` §当前持仓 仍写着 cf2701 持仓中 —— **该段过期**（9/11 已平），待更正。

## 训练
**本日整块未跑**（机械实查，非用户自报）。周三轮转应为 **B + E + G + D + J** + D-Drill，但三条记录文件里**一条 9/16 的都没有**：
- `drill_state/history.json` 最后写入 **9/15 20:46**（最后四次 E/C/H/I 全在 9/15，且是当晚 20:42–20:46 补跑的）
- `drill_state/d_drill.json` 最后 session **9/15**（9/12→9/15 之间已缺 9/13、9/14）
- `data/quiz_history.json` 最后 **9/11** → **9/14、9/15、9/16 三天无记录**（收尾① 每日必做项）

📌 **记账口径**：**"没跑"是机械可查的事实，与"跑了但没报分数"是两回事**——故此处不留【待填】，直接记未跑。今天之前教练一直把训练那栏当【待填】等她报，其实文件里查得到，**该查没查**。

⚠️ **是否 burnout 暂不判**：burnout 五信号里「连续消失 3+ 天」对应的是**所有项**；今日是 drill 缺 1 天（9/15 有跑）+ quiz 缺 3 天，**够不上判定线，但够得上预警**。另注：9/15 那次是**当晚 20:42 补跑**——「拖到晚上补」与「整块没跑」是同一根链条的两端。

## Sinclair
【待填】A 线程（问题驱动）+ B 线程（日志倒灌·重读驱动）本日跑了没？如果跑了，填落点章节页码。

## Douglas
【待填】读 1 段 + 一句「今天有没有这个心理」。

## 身体
【待填】壶铃 + **清醒度 1-5**（9 月至今仅 3 天有值，这一格别再空着）。

## 系统变更

### 🔴 Excel 往返毁数据事故（本日最大）
- **现象**：`data/iv_direction_pred.csv` 被 Excel 打开后保存（20:28），静默抹掉 **124 格已回填数据**（**早先记为"30 格"是错的——30 是行数不是格数，教练把行当格数**）：
  - 9/7 批 **46 格** = `actual`10 + `actual_delta_pp`10 + `actual_reason`10 + `user_correct`8 + `coach_correct`8（2 行 flat 不填）
  - 9/8 批 **48 格** = 同上，`*_correct` 各 9（1 行 flat）
  - 9/15 批 **30 格** = `coach_pred`10 + `coach_reason`10 + `compare_date`10
- **机制（已从推理升级为实证）**：不是"读到我 append 之前的快照"（那是错的），而是 **Excel 存回了一份 9/15 收尾之前（commit `67761c7` 之前）的旧图**。举证：逐格比对 `67761c7` 相对其父提交写入的格子 vs 被抹掉的格子——
  - **9/7 批：commit 写入 46 格，被抹 46 格，逐列完全一致**
  - **9/8 批：commit 写入 48 格，被抹 48 格，逐列完全一致**
  - 9/15 批：`67761c7` 整行新增该批，被抹的正是那批新增里的 `coach_*`+`compare_date` 三列（`user_*` 两列她自己填的，存活）
  - → **Excel 的记忆里，收尾那次 commit 灌进 9/7、9/8 的 94 格从来没存在过**
- **用户独立佐证**：她当时说「**我填的时候没有数值**」——那不是"那列本来就空"，是**在报告 Excel 看到的图不对**。当时被读成前者，漏掉了这条最强的证据。
- **顺序悖论（本案核心）**：教练 09:2x 写入 → 用户 20:28 保存。**后发生的操作抹掉了先发生的数据**，因为 Excel「保存」写的是**它内存里的旧图**，不是硬盘上的文件。
- **未查明**：Excel 为何会攥着一张**一天前**的旧图。最像的解释是「9/15 用它填过 9/15 预测、窗口一直没真关，9/16 在同一窗口接着往下填」——**标 ⚠️ 推测，无证据，不当结论**。已证实的只有：旧图日期被钉死在 **9/15 白天**。
- **恢复**：以 `git show HEAD:data/iv_direction_pred.csv`（110 行，9/15 收尾那次的干净版本）为基，拼上今日 10 行 → 120 行。复核：12 列一致 / 9/7 批 actual 10/10 / 9/8 批 10/10 / 9/15 批 coach 10/10 + `compare_date=2026/9/22` 全回。损坏版备份 `/tmp/ivpred_excel_damaged_2031.csv`。
- **根因（结构性）**：这份 CSV 一直靠**人手编**。手编 + 表格软件 + 事后才发现 = 没有任何一道闸。

### 🔴 `reason` 列长期冻结（取证副产物，比 Excel 事故更值得记）
- 逐批比对后发现：用户的 `user_reason` **不是今天才复用，是长期模板**——
  - `减产落地均值回归`（cf）出现在 **5 批**：9/8 · 9/9 · 9/10 · 9/15 · 9/16
  - `无事件催化…`（i）出现在 **5 批**：9/1 · 9/2 · 9/3 · 9/7 · 9/8
  - `wasde快来了`（m/c）出现在 9/10 ✓（WASDE 9/11，确实将到）、**9/15 ✗、9/16 ✗**（WASDE 已过 4–5 天）
  - `原油96撑IV`（ru）在 9/15、9/16 仍在用，而 9/14 原油已是 **107.32**
- **后果**：9/15、9/16 两批的 key 上，`reason` 列**不含当日信息**。9/23 对照时判的不是「她当天怎么想」，而是「她 8 天前写的那句话」。
- **与既有规则的冲突**：`reason 冻结铁律`（9/10 立，防事后改）在实现上把「每天重写」一起冻住了。**该条需要打补丁**：冻结的是「已写入的不许回头改」，不是「新批次可直接复用旧文」。
- **逐字比对（脚本，日期口径 `2026/9/16` 无前导零）**：9/16 批 **10/10** 行的 `user_reason` 与 9/15 批**逐字相同**（相似度 1.00），最早可追至 **9/8**（cf 4 次 · ma/au/sr/ru 3 次 · m/c/ta/i 2 次 · rm 1 次）。
- 教练侧自检：`coach_reason` 跨批重复 **0 条**（干净）。
- **处置 = 留痕，不重写**（B1 裁定）：新增 `flag` 列（第 13 列），10 行标注 `〔reason 逐字复用·距上次 9/15·最早 YYYY/M/D·历史 N 次〕`。**原 12 列逐格比对 0 差异、行数不变**（脚本核实）。理由：今晚重写＝用"9/16 晚被我点过之后的她"覆盖"9/16 早 9:15 的她"，把一条不可评分的解释伪装成可评分的预测；留痕则保住方向判断（7 up / 3 down）照常计分，只给理由栏挂价签。与 9/9 批 `〔非独立填〕`、8/27-9/2 批 `〔跨期补写〕` 同类——**读法标注，不动内容**。
- ⚠️ 教练本日自摆乌龙一次：首版比对脚本把日期写成 `2026-09-16`，而表内是 `2026/9/16`（无前导零）→ 输出"复用 0 行"，据此误判"不是逐字复制是变体"。**修正日期解析后 10/10**。教训同 [[verify-not-infer]]：**脚本报 0 ≠ 事实为 0，先验判据本身**。

### IV 方向预测 9/9 批回填（收尾⑨，本日到期）
- 09:30 同钟口径，Δ 由脚本从 `iv_history.csv` 实算（非手写）。
- **本批 10 行结果**：m +0.38✓ / c −0.85 / rm −0.95 / **ta +11.26** / ma +4.24 / au −1.05 / cf −0.45 / sr −2.12 / **i −0.12 flat** / ru −3.30
- **user 3/9 · coach 4/9**（flat 剔 1）。
- ⚠️ **`actual_reason` 非独立填**：本日为查 `reason` 复用问题，教练把全表 `user_reason` 打印过一遍（含 9/9 批）——**已违反「独立填」条件**，10 行均标 `〔非独立填·教练已见 user_reason〕`。读时打折。
- **两个最大偏差**：`ta +11.26pp`（用户与教练**同时押 down** —— Hormuz 升级把能化 IV 整体抬走）、`sr −2.12pp`（**同时押 up** —— 暴雨窗就在对照日当天，IV 反而降到 9.85%）。
  → **sr 这条值得单记**：**事件临近 IV 反降**，与 7/30 FOMC「全天零 crush」构成相反方向。两者合起来说明「事件 → IV」路径**不单调**，不能靠单一经验外推。

### 🟢 两条新规落地（由 Excel 事故倒逼）
1. **`CLAUDE.md` 新增「`data/*.csv` 的打开与写入（9/16 立）」**（非门级，用户两次点头）：
   - 🔴 禁止用 Excel/Numbers/WPS 直接打开 `data/` 下原文件（**连"只是看看"也不行**）
   - ✅ 要看 → `./tools/view_csv.sh data/<文件>.csv`（新脚本）
   - ✅ 要改 → 发教练，脚本落盘后**打印回核**（= B2 裁定）
   - 反方 3 条随附（多一步命令的摩擦 / 只读挡不住"另存为" / 只管 `data/` 下的 CSV）
2. **新脚本 `tools/view_csv.sh`**（已建，三条测试通过）：拷带时间戳副本到 `/tmp` → `chmod 444` 设**只读** → 用表格软件打开**副本**。原文件全程只被读。含安全闸：只允许操作 `data/` 下文件。测试时把 `open` 打桩（不真弹窗口），前后 md5 一致。
   - **设计要点**：不是靠"记得别点保存"，是**机制上让 Excel 存不回去**——同「破规则的规则=没规则」，别靠自觉靠结构。
3. **`task-template.md` 收尾新增 ⑩「`data/` 外部改写校验」**：`git diff --stat data/`，每处改动都要能说出"这是我今天做的哪一步"。**为何 ⑧⑨ 在位仍被毁**——两者都只管"我自己的回填有没有落盘"，管不了"文件被外部程序整份重写"。

### 其他
- **教练盲填铁律首次执行**（9/15 立）：本批教练列在她提交前落盘 ✅。附带发现：**核对 CSV 格式时读了文件尾部，看到了 9/15 批的 user 列**——字面铁律未破（今日列是空的），但按立规精神有锚定风险。**下次核对格式只 `head -1` 读表头。**

## 讨论
【可补】Excel 事故 + reason 列冻结 → 落盘流程要不要改成「用户发值 → 教练脚本落盘 → 打印回核」。

## 数据状态
- **IV 采集**：今日 09:15 / 09:30 / 14:50 **三采全**（各 10 条）✅
- **Data 门 = 41/60**（脚本实算，门=每品种最小唯一日期，绑定品种 **cf=41**；9/15 为 40 → **+1**）
  各品种：cf 41 / au 43 / i 43 / ru 43 / sr 43 / rm 46 / c 47 / m 48 / ma 48 / ta 48；总唯一 48
- **deep_otm 两文件今日均有数据** ✅（`trading` 13338 行 / `eod` 1794 行，最新日期 2026-09-16）
- **IV 方向预测全样本**（脚本打印）：总 **120** · 已回填 **90** · flat **14** · 计分 **76**
  → **user 47/76 = 61.8% · coach 45/76 = 59.2%**
  报数格式：`61.8%（剔平 76 例，flat 14）`

## 今日反思
【用户写】两行自问：**今天我有没有主动放弃的收益机会？（包括没去争取的）怕麻烦/回避少赚了吗？**

## 明日待办
【用户写，教练候选 2 条】
1. **收尾⑨**：9/10 批 IV 预测对照日 = **9/17**（10 行待回填）
2. **9/17 双节点**：iv_collector 60 天品种评估（取料 `docs/variety-cards.md`）+ **H 题库扩题**（15 道已全见过，分数失区分度）

## 今日学到
（用户自写）
